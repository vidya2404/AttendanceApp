<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendanceapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactsActivity" />
        <activity android:name=".DetailsUI" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".TableReport" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".CreateTable" />
        <activity android:name=".CreateBtnUI" />
        <activity android:name=".TakeAttendance" />
        <activity android:name=".Unit_Test" />
        <activity android:name=".UnitTestTable" />
        <activity android:name=".MainActivity"/>

    </application>

</manifest>